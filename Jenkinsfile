pipeline {
    agent any

    stages {
        stage('this is 1 sketching stage of your application') {
            steps {
                echo 'the first stage of application development is usually ideation or discovery, which involves gathering input and research to define the app spurpose and requirements.'
            }
        }
         stage('this is 2. planning stage of your application') {
             steps {
                 echo 'the planning stage involves understanding user needs: studying end-user needs and matching them to user expectations.'
                 echo 'its an important step in the application development process because it establishes the apps feasibility and gives the team flexibility.'
            }
        }
         stage('this is 3. building stage of your application') {
             steps {
                 echo 'the third stage of application development is the building stage, this stage involves coding the app, which can be broken down into two parts: front-end and back-end development.'
                 echo 'the user interface that users interact with. front-end developers make sure the interface is visually appealing and user-friendly.'
                 echo 'the core functional component of the app that manages operations, calculations, and efficiency. the back-end is also known as the server-side of the app.'
            }
        }
         stage('this is 4 testing stage of your application') {
             steps {
                 echo 'this stage involves testing the application for bugs, performance, compatibility, and user experience.'
                 echo 'an application is ready for testing once the team is done with all the previous phases.'
                 echo 'according to the test plan, the testers execute test cases.'
                 echo 'they also identify, detect, and log the defects, thus reporting the bugs.'
            }
        }
         stage('this is 5 deployment stage or final stage of your application') {
             steps {
                 echo 'this stage involves releasing the application and continuing to improve it with new features.'
                 echo 'its the final stage of the software development life cycle and involves making an application available for use.'
                 echo 'making the application available for use on a server, device, or in a production environment.'
                 echo 'monitoring the application performance and responding to any issues.'
                 echo 'some deployment strategies include-blue/green deployment which involves keeping two versions of the software live at once, with traffic being redirected from the current version to the new version once ready.'
            }
        }
    }
}
